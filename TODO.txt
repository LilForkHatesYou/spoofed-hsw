const se = new WebSocket("ws://127.0.0.1:4968");

se.onmessage = async (message) => {
    const payload = message.data;
    window.focus();

    let token = "";
    
    token = await hsw(payload);
    console.log(token)
    se.send(JSON.stringify(token));
};